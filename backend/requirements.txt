requests==2.31.0
python-dotenv>=1.0.1,<2.0.0
psycopg[binary,pool]>=3.2.6
pytest>=8.0.0

# RAG (retrieval-augmented generation): local embeddings + local vector store,
# no API key or external server required.
sentence-transformers>=3.0.0
chromadb>=0.5.0
pypdf>=4.0.0